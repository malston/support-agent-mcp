# Secrets the committed .mcp.json references via ${VAR}. Real values live here
# (this file is .gitignored); .mcp.json carries only the variable names.
SUPPORT_ORDERS_API_KEY=
SUPPORT_ORDERS_DB_URL=
SUPPORT_REFUND_SIGNING_SECRET=

# Only needed for the LIVE router (ModelRouter in live.py). The deterministic
# router and the entire test suite run WITHOUT a key.
ANTHROPIC_API_KEY=
